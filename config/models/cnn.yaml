# 1D-CNN local motif detector. return_sequences -> per-residue output.
name: cnn
encoder: cnn
grid:
  reducer_dim: [64]
  filters: [[64, 64], [128, 64]]
  kernel_size: [3, 5]
  dropout: [0.3]
  learning_rate: [0.001]
  batch_size: [64]
